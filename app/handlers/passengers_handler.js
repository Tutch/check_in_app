'use strict';
const mongoose = require('mongoose');
const passenger_models = require('../models/passengers');

module.exports = {
    bookSeat: (req, res) => {
        res.send('!');
    },
    reserveSeat: (req, res) => {
        res.send('!');
    }
};
